$(function () {
    // 网站标题
    window.document.title = config.siteTitle;

    // 网站顶部导航
    // if (config && config.menu) {
    //     config.menu.forEach(function (val, idx, arr) {
    //         $('#pageMenu').appendTo();
    //     });
    // }
});
